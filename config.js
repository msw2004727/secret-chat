/* ============================================================
 *  設定檔 — 整個專案「只有這個檔案」需要你動手改
 *  ============================================================ */

window.CONFIG = {
  deviceHistoryEnabled: false, // 停用設備足跡與儀表板設備明細

  /* ---------- 1. Firebase 設定（從 Firebase 主控台複製貼上） ----------
   * 位置：Firebase 主控台 → 專案設定(齒輪) → 一般 → 你的應用程式 → 設定
   * 這段資訊是公開的、可以放在前端，不算機密。
   * ------------------------------------------------------------ */
  firebase: {
    apiKey: "AIzaSyCrJ2jAb5znad43OsqKSQDFI9gc0ayun8E",
    authDomain: "daily-notes-7bb64.firebaseapp.com",
    databaseURL: "https://daily-notes-7bb64-default-rtdb.asia-southeast1.firebasedatabase.app",
    projectId: "daily-notes-7bb64",
    appId: "1:731968413005:web:28d7ac85e60ccde8ca2b04",
    /* ⚠️ 照片功能一定要有這一行，少了它送照片會出現
          "No default bucket found"。

       名字去哪裡看：Firebase 主控台 → Storage，畫面最上方那串
       gs://xxxxx 就是（去掉 gs:// 之後貼在這裡）。

       2024 年 10 月之後開的專案是 <專案ID>.firebasestorage.app，
       更早的是 <專案ID>.appspot.com —— 兩種都有，不要用猜的。 */
    storageBucket: "daily-notes-7bb64.firebasestorage.app"
  },

  /* ---------- 2. 密碼設定 ----------
   *
   * ⚠️⚠️ 這個檔案會被公開部署，任何人都能用瀏覽器打開它。
   *       絕對不要在這裡寫下任何真實密碼，連註解裡都不行。
   *
   * 下面存的是密碼經過 31 萬次運算後的「指紋」，不是密碼本身。
   * 清單上的任何一組密碼都能進去，但會進到**各自獨立的房間**：
   * 房間位址和加密金鑰都是從密碼推導出來的，不同密碼之間
   * 看不到彼此的訊息，連解密都解不開。房間不用預先建立，
   * 第一次有人進去發訊息就會自動產生。
   *
   * 想加房間或換密碼？打開 密碼產生器.html（這個檔案不會被部署），
   * 一次可以產生多組，把輸出貼回來即可。
   * 密碼分別是哪一組，記在 密碼清單.txt（同樣不會被部署）。
   * ------------------------------------------------------------ */
  appSalt: "sc.v1.a27e643a61e60e528a4e18c8",
  pbkdf2Iterations: 310000,

  /* 房間分兩種：
   *
   *   私人房（沒有 open）  1 對 1。有已讀回條、一鍵清除、歷史訊息鎖，訊息不自動刪。
   *   多人房（open: true） 1 對多。進去要先取暱稱與顏色；訊息只留最近 48 小時；
   *                        沒有已讀回條、沒有一鍵清除、只能刪自己的訊息、
   *                        在線只顯示「有人」，不顯示幾個人也不顯示是誰。
   *
   * ⚠️ 不要把用過的私人房改成 open —— 那等於把既有的 1 對 1 對話攤給所有人看。
   *    要開多人房一律用全新的密碼（＝全新的房間位址）。
   */
  passwords: [
    { role: "room-5", fingerprint: "35bbe1668fb91ab1b5784ec706a8e973599d6b8b655e6c8d24566263c5c3856d" },
    { role: "room-1", fingerprint: "ee6ea23fc587214103ca3b6c104f1d4153cf6014a4093fa0ececf654c32fc4e3" },
    { role: "room-2", fingerprint: "a2bfb5d0f10755887da569fa446df27709fe8bb9247cfbbb38b997e2d0499b9b" },
    { role: "room-3", fingerprint: "1ad595b0d2b6f0e8dbf8cda122a9d2fae73e581b254365efe6f221f94a9ae6e8" },
    { role: "room-4", fingerprint: "ca3fd60f0890eb5e6c03e286c7b97f7cebbd2d3d65baca0bd29aa1f4b6725156" },

    { role: "open-1", open: true, fingerprint: "31c173159f9d1939835087bb2f3467848c7a72f27bf1f25ac849e54249dfd2d7" },
    { role: "open-2", open: true, fingerprint: "81f12d86d144d6476bac77869fdd5a7311912f656ce8175ff53cd3675b0eb5fa" },

    // 管理儀表板。輸入這組密碼進的是儀表板，不是聊天室。
    // ⚠️ 儀表板本身「什麼都不存」—— 房間密碼是你當場貼上的，只活在記憶體。
    //    所以就算有人破解了這組密碼，看到的也是一個空畫面。
    { role: "admin", fingerprint: "1fe365efbcb9ff6fb4b4eb10a5ae5f7e8eaeed939e066d46039e12fb883f4671" }
  ],

  /* ---------- 3. 行為設定 ---------- */

  // 聊天室閒置幾秒後啟動保護（沒有滑動、打字、點擊都算閒置）
  idleSeconds: 15,

  // 閒置後要做什麼：
  //   "cover" = 蓋上毛玻璃遮罩，輸入密碼就回到原本的對話（推薦，不會中斷）
  //   "exit"  = 直接退回偽裝首頁，要重新進房
  idleAction: "cover",

  // 顯示「已送達 / 已讀」兩段式提示。
  //   已送達 = 訊息真的到了對方的裝置、而且解得開（他人在房裡就算，遮罩蓋著也算）
  //   已讀   = 對方真的看到了（畫面亮著、遮罩沒蓋、而且捲到最底才算）
  //
  // 為什麼要分兩段：手機閒置 15 秒會蓋遮罩、切 App 會直接退房，
  // 只有「已讀」的話十次有九次看不到任何回饋，會誤以為訊息根本沒送出去。
  //
  // ⚠️ 打開等於讓對方知道你幾點在看手機（會洩漏作息），不想要就改成 false。
  // 兩邊都要設一樣才有意義：設成 false 時，你不回報也不顯示。
  showReadReceipt: true,

  // 未讀提示：偽裝首頁的鈴鐺點一下，有新訊息就出現一顆小紅點。
  //   ‧ 每個房間各自有開關，預設全部關閉，要在設定面板自己開
  //   ‧ 開了之後這台裝置會存下該房間的位址（不含密碼、不含金鑰）
  //   ‧ 首頁載入時不連線，只有你點鈴鐺才連
  //   ‧ 改成 false 就整個功能消失（設定面板那一列也不見）
  //   ‧ v36 起「多人房也有」，而且不再需要 showReadReceipt ——
  //     私人房用雲端的已讀位置算，多人房用這台裝置本機記的「上次讀到哪」算
  unreadBell: true,

  /* 緊急退出（連點／劇烈搖晃／翻面／Esc）時，要不要順便把鈴鐺清單整份清掉。
     預設 false —— 使用者選的。

     ⚠️ 想清楚再改成 true：
        鈴鐺**本來就**要把房間位址長期存在這台裝置上才能運作，平常那幾組位址一直都在。
        所以這個開關真正的差別只有「手機被搶走的那一刻要不要抹掉」。
     ⚠️ 位址外洩的後果：對方**解不開任何內容**（金鑰是密碼推導的，不在裝置上），
        但安全規則允許已登入的人寫入，所以他可以把那間房的對話刪掉。
     ⚠️ 設成 true 的代價：每一次緊急退出後，所有房間的鈴鐺都要重新開一次。
        v36 之前就是這樣（而且只有連點／搖晃／翻面會清，Esc 不會，兩條路不一致）。 */
  panicClearWatch: false,

  // 每次往上捲動載入幾筆訊息
  pageSize: 30,

  // 每一則訊息最多活多久（小時）。0 = 不自動刪，訊息一直留著。
  //
  // 預設 0：清空改成自己決定時機 ——
  //    ‧ 聊天室裡的「一鍵清除」（雙方畫面同時清掉）
  //    ‧ 管理儀表板的「清空」（可以遠端清任何一間，房裡的人會即時同步）
  //
  // ⚠️ 關掉自動清理的代價：訊息會一直累積，占用 Firebase 免費方案的
  //    儲存空間（上限 1GB）與每月下載量（10GB）。貼圖特別吃空間，
  //    一張壓縮後大約 20～60KB。記得偶爾用儀表板看一下訊息數。
  //
  // 想改回自動清理就填時數（例如 48）。那是「滾動視窗」不是「整間房定期清空」：
  // 按每一則訊息自己的時間戳算，超過的個別刪掉，未超過的完全不動。
  // 觸發時機是有人進房、或在偽裝首頁點鈴鐺查未讀時（免費方案沒有排程可用）。
  roomTtlHours: 0,

  // 多人房（open: true）專用的保留時數。私人房不受這個設定影響。
  //
  // ⚠️ v26 起預設 0（不自動刪）—— 使用者決定的。功能本身完全保留，
  //    填時數（例如 48）就會立刻恢復滾動刪除，程式碼那條路徑一直都在。
  //
  // ⚠️ 改成 0 之後多人房的訊息會一直累積，而且「一鍵清除」在多人房是藏起來的
  //    （一個人按下去等於把所有人的紀錄都刪掉，太危險）——
  //    唯一的清除途徑是用 admin 密碼進儀表板遠端清空。這是刻意的。
  //
  // ⚠️ 填時數的話，清理是「有人進房時順手做」，免費方案沒有排程可用。
  //    整間房幾天沒人進去的話，超過時限的訊息會留到下一次有人進來才被刪掉。
  //    所以聊天室上方那條提示的措辭是「只保留最近 N 小時」而不是「滿 N 小時就會消失」。
  openRoomTtlHours: 0,

  // 貼圖壓縮後的最大邊長（像素），越小越省流量
  stickerMaxSize: 320,

  // 貼圖壓縮品質 0～1
  stickerQuality: 0.82,

  /* ---------- 3b. 照片 ----------
   *
   * 照片跟貼圖是兩回事，一張照片會產生兩份：
   *
   *   縮圖  photoThumbSize  → 包在訊息密文裡，跟訊息一起存在 Realtime Database
   *   原圖  photoMaxSize    → 加密後上傳 Cloud Storage，點開才下載
   *
   * 這樣捲動聊天室只會載縮圖（一張約 15～25 KB），
   * 原圖（約 200～600 KB）只有真的要看的時候才付流量。
   *
   * 費率差很多：RTDB 下載 $1/GB，Cloud Storage 下載 $0.12/GB，
   * 而且 Storage 每天有 1 GB 免費額度。
   *
   * ⚠️ 要先在 Firebase 主控台啟用 Storage（位置選 asia-southeast1，跟資料庫同一區）。
   *    沒啟用的話送照片會失敗，但聊天室其他功能完全不受影響。
   * ------------------------------------------------------------ */

  // 原圖的最大邊長。比這個小的照片「不會被放大」，比例永遠不變。
  // 實測（一般照片、WebP 品質 0.85）：1600px ≈ 150 KB、2048px ≈ 250 KB、2560px ≈ 400 KB
  photoMaxSize: 2048,

  // 原圖壓縮品質 0～1。0.85 是「看不太出來」與「檔案不爆炸」的分界，
  // 拉到 0.9 檔案會直接翻倍，肉眼幾乎沒差。
  photoQuality: 0.85,

  // 泡泡裡那張縮圖的最大邊長與品質。它會永久跟著訊息，所以要小。
  photoThumbSize: 480,
  photoThumbQuality: 0.7,

  // 單張原圖上傳的大小上限（MB）。超過就不送，避免誤傳巨檔。
  // ⚠️ 改大的話，storage.rules 裡那道上限也要一起改（現在是 64 MB）。
  photoMaxUploadMB: 6,

  /* ---------- 影片（v23） ----------
   *
   * ⚠️ 影片「刻意不壓縮」。在瀏覽器裡轉檔要動用 WebCodecs 或 ffmpeg.wasm，
   *    手機會發燙、耗電、還要等很久 —— 違反這個專案「低耗能」的前提。
   *    所以策略是「設上限、超過就擋下來請使用者自己剪」，
   *    絕對不要偷偷降畫質（使用者會以為傳出去的是原檔）。
   *
   * ⚠️ 也「不能邊下載邊播」。整支影片是加密成一整塊的，
   *    必須整支下載完、解密完才播得出來。所以上限不只是錢的問題，
   *    更是「對方要盯著進度條等幾秒」的問題。
   *    50 MB 在 4G 上大約要等 10～20 秒。
   *
   * 參考：iPhone 1080p30 大約每秒 1.2～1.7 MB，4K 再乘 3～4 倍。
   *      30 秒 1080p ≈ 40 MB，剛好卡在上限附近。 */
  videoMaxSeconds: 30,
  videoMaxUploadMB: 50,

  // 封面（抽出來的那一幀）。跟照片縮圖一樣會永久跟著訊息，所以要小。
  videoPosterSize: 480,
  videoPosterQuality: 0.7,

  /* 抽第幾秒當封面。⚠️ 不要用 0 —— 很多手機影片的第一幀是全黑的
     （自動曝光還沒跟上），抽出來的封面會是一片黑。 */
  videoPosterAt: 0.6,

  /* 原圖在雲端保留幾天 —— ⚠️ 這個數字「只是拿來顯示」的。
     真正在刪檔的是 Google Cloud Storage 的生命週期規則
     （主控台 → Cloud Storage → 該 bucket → 生命週期 →
       年齡 7 天 + 前置字元 media/ → 刪除物件）。
     兩邊要填一樣，不然畫面上寫的天數會跟實際不符。

     原圖過期之後縮圖還在，泡泡照樣看得到圖，點下去會說「原圖已過期」。 */
  mediaTtlDays: 365,

  /* 抓原圖／原檔等多久就放棄（秒）。
     ⚠️ 不可以省。Firebase Storage SDK 自己的重試預算是以分鐘計的 ——
        沒有這道上限，網路一出問題畫面上就是一顆永遠轉不完的圈，
        而且從頭到尾不會有任何一句訊息。 */
  mediaTimeoutSeconds: 30,

  /* 管理儀表板：每隔幾秒自動重查一次（0 = 關閉自動更新）。
     ⚠️ 儀表板是「一次性掃描」不是即時訂閱 —— 不重查的話畫面上的數字
        會停在你按下查詢的那一刻，而在線人數這種東西幾秒就變了。
     ⚠️ 別調太短：每一輪都要對每一間房發四個請求。60 秒對「看著它變化」
        已經夠即時，而且分頁切走時會自動暫停。 */
  adminRefreshSeconds: 60,

  /* 緊急退出之後要跳去哪個網站（留空 = 只回本機偽裝首頁，不離站）。
     ⚠️ 只有「硬觸發」會跳：連點、劇烈搖晃、翻面朝下、Esc。
        切 App 與閒置**不會**跳 —— 那是每天做幾十次的事，
        跳走之後要回來得重打網址，代價太大。
     ⚠️ 順序是「先本機瞬間切掉、再導頁」，導頁只是加在後面，不可以取代它。
        瀏覽器在新頁面畫出第一幀之前會一直顯示舊頁面 ——
        只導頁不先切的話，那一兩秒螢幕上還是聊天室，
        緊急退出最重要的那一瞬間就整個賠掉了。
     ⚠️ 沒網路時不會跳：離線導頁會落在瀏覽器的「無法連上這個網站」錯誤頁，
        那比偽裝首頁可疑得多。
     ⚠️ 換網址時挑「看起來像瀏覽了一陣子」的頁面（例如某個列表頁），
        首頁看起來像剛打開。 */
  panicUrl: "https://www.threads.com/",

  /* 本機暫存的上限（MB）。看過的原圖／影片會留在這台裝置上，再點就不連網。
     超過上限會從「最久沒看的」開始丟掉。設定面板裡可以手動清空。
     ⚠️ v23 起影片也會進來，一支就可能 50 MB —— 所以從 150 拉到 250。
        不要再往上拉太多：iOS 對 IndexedDB 有配額，塞爆會整個寫入失敗。 */
  mediaCacheMaxMB: 250,

  /* ---------- 3c. 新訊息推播 ----------
   *
   * ⚠️ 預設「每一台裝置都是關的」。要收推播的人自己去設定面板開，
   *    不開的人完全不受影響，也不會留下任何資料。
   *
   * 運作方式：
   *   ① 你在設定裡打開開關 → 瀏覽器問你要不要允許通知
   *   ② 允許之後，這台裝置的「訂閱位址」寫進 rooms/<房號>/push/<裝置代號>
   *   ③ 有新訊息時，Cloud Function 會通知「此刻不在房裡」的裝置
   *
   * ⚠️ 通知內容永遠是同一句固定文字（寫在 sw.js 裡）。
   *    訊息是端對端加密的，伺服器解不開，只知道「有東西進來了」。
   *
   * ⚠️ iPhone 一定要先把網站「加到主畫面」，從主畫面那個圖示打開才收得到。
   *    Safari 分頁裡開了也沒用（Apple 的限制）。Android 和電腦不用裝，一般分頁就行。
   *
   * ⚠️ 這個功能需要部署 Cloud Function（functions 資料夾），沒部署的話
   *    開關打得開、訂閱也寫得進去，但永遠不會有通知送出來。
   * ------------------------------------------------------------ */

  push: {
    // 整個功能的總開關。false 的話設定面板連那一列都不會出現。
    enabled: true,

    /* VAPID 公鑰。這是公開的，放在這裡沒問題。
       ⚠️ 對應的私鑰在 functions/.env，那個絕對不能公開。
       ⚠️ 兩邊一定要是同一組 —— 換了其中一邊，所有人的訂閱會全部失效，
          而且不會報錯，只是永遠收不到通知。 */
    publicKey: "BN5O3jVScwuNkziD2EAtqQbbFq-Tq5hREBEamThPLbfETpCvAOApHd45vCB5dGV38eC5XKMUvIBzN75z2CVvJIY",
  },

  // 首頁 Logo 文字（想改成別的字也可以，會自動套用同樣的彩色樣式）
  logoText: "Google",

  // 分頁標題偽裝
  disguiseTitle: "Google",

  // 是否啟用毛玻璃遮罩（解除需重新輸入密碼）
  blurOnBlur: true,

  // 視窗失去焦點時要不要立刻蓋遮罩。
  // ⚠️ 預設 false。手機上叫出／收起輸入法都會送出「失焦」事件，
  //    開啟的話等於每打完一句話就要重新解鎖一次，完全沒辦法用。
  //    關掉之後保護仍然完整：閒置 15 秒會上鎖，切換 App／鎖螢幕會直接退回首頁。
  //    只在電腦上使用、又很在意旁人瞄螢幕的話，才建議改成 true。
  coverOnBlur: false,

  // 按 Esc 立刻退回偽裝首頁
  panicKey: "Escape",

  /* ---------- 4. 緊急隱藏 ---------- */

  // 切到背景 / 切換 App 時，直接退回偽裝首頁
  // （比毛玻璃更安全：手機的 App 切換器截圖拍到的會是乾淨的 Google 頁面）
  hideOnAppSwitch: true,

  // 在畫面上任意位置連點幾下就緊急退出。設 0 關閉。
  // 判定條件：兩次觸碰要在時間窗內、而且落在相近位置（滑動捲動不算）。
  // 輸入訊息與輸入密碼的框內不計入，才不會打字打到一半被踢出去。
  panicTaps: 0,             // 停用連點／雙擊緊急退出
  panicTapWindowMs: 500,
  panicTapRadius: 44,        // 兩次觸碰最多相距幾像素才算「連點同一處」

  // 劇烈晃動就緊急退出（手機被搶走的瞬間會有很大的加速度）
  panicShake: true,
  shakeThreshold: 22,        // 數字越小越靈敏，20～28 之間比較合理

  // 手機螢幕朝下放置就緊急退出
  panicFaceDown: true,

  // 進入聊天室後要不要把網址改寫成別的路徑。
  // 保持 "" 代表不動網址 —— 網址永遠是最乾淨的 https://你的專案.web.app
  fakeUrl: "",

  /* ---------- 5. 位置回報（v41）---------- */

  /* 偽裝首頁上一顆平常看不到的大頭針：長按 Google logo 才會淡入，
     按下去把座標「密封」後丟上雲，只有儀表板解得開。

     ⚠️ 總開關預設關。設 false 時那顆按鈕**根本不會進 DOM** ——
        不是用 CSS 藏起來，是連生都不生。 */
  locationReport: true,

  /* 位置回報的兩把鑰匙，都由「位置金鑰工具.html」產生，兩行都貼進這裡。

     locPub  = 公鑰。偽裝首頁用它把座標「封起來」，它只能塞進去、解不開任何一筆。
     locPriv = 私鑰，**已經先用你的 admin 密碼鎖過一層**。
               進儀表板時用你打的那組密碼自動解開 —— 所以你只要記密碼，
               換手機、換電腦都不必做任何設定。

     ⚠️ 這兩行必須是**同一次**產生的，配不起來就解不開（畫面會直接告訴你）。
     ⚠️ 換 admin 密碼 = 要重跑金鑰工具、重貼這兩行，否則舊紀錄再也打不開。
     ⚠️ 忘記 admin 密碼 = 所有位置紀錄永久解不開，跟房間密碼一樣救不回來。

     ⚠️⚠️ 已知的取捨（使用者 2026-08-24 明確選擇「先不用做到這麼嚴謹」）：
        這個檔案是公開的（https://你的網域/config.js 誰都下載得到）。
        locPriv 雖然鎖過，但公開發布之後，別人可以把它抓回家「完全離線」慢慢試密碼 ——
        不用連你的網站、不留痕跡、你也不會知道有人在試。
        8 碼純數字約一億組，在單張高階顯卡上大概一個半小時試得完。
        換來的是「只要密碼、零設定、換裝置就能用」。
        哪天想把這道補回來，就是改成「locPriv 不放這裡、改成進儀表板貼一次」。 */
  locPub: "BK2lUExOfqvjgS/84r5F7SRZ3k7skkCpju/WKte4OlpzumBAS9UD9+KM+I3c3Ycg7VUvpY+ke/LV8AT1cD29Etc=",
  locPriv: "mPwgmHtWe3ZYNjExIxAW1A6JzRY+TadgIT0UPAWFBaMTyGaCcFBdQkI731gP60+NexUzSGR4OSdyzKxwQMpauuvutmxCU40420yUE/dp8k3kEnhCq0GMkmt5lW4zViFytwKXGLY3hxvZ4ZFxhuovER0z5j5XMkUwYePX1FZkB4VBMP9r7nOI9KxzHNhzzFc9+M9RWBE2i3WpUmoK3zeX4uvQkQJR8g==",

  /* 同一台裝置兩次回報至少要隔幾秒。
     ⚠️ 這只擋手滑，**不是安全機制** —— 紀錄在 localStorage，清瀏覽器資料就重置。
        擋惡意洗版的是 App Check，不是它。 */
  locCooldownSeconds: 60,

  // 位置紀錄保留幾天（超過的由儀表板進來時順手刪掉）
  locKeepDays: 7,

  // 長按 Google logo 幾毫秒，大頭針才淡入
  locLongPressMs: 1500,

  // 大頭針出現後幾毫秒沒動作就自己消失
  locRevealMs: 5000
};

/* ═══════════ LINE 審核登入（v61）—— 由 Claude 於 2026-09-18 附加在檔尾 ═══════════
   用「賦值」的寫法加在物件外面，不動你上面那一整包設定。
   ⚠️ lineAuthUrl 要等 functions 部署完、拿到網址之後自己填（部署說明第 4 步）。 */
window.CONFIG.lineChannelId = "2011650445";                 // LINE Developers → Notes channel
window.CONFIG.lineRedirect  = "https://sb.02251121.com/";   // 跟 LINE 後台的 Callback URL 一字不差
window.CONFIG.lineAuthUrl   = "https://lineauth-pbsqq3hcja-as.a.run.app";                           // ⚠️ 部署後填：https://lineauth-xxxxxxxx-as.a.run.app
window.CONFIG.lineDenyUrl = "/pending.html"; // 未核准時顯示等候頁
window.CONFIG.panicSignOut  = false;                        // 緊急退出要不要順便登出 LINE 身分
