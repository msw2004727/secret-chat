# Project instructions

- Respond in Traditional Chinese unless asked otherwise.
- Canonical source: public repository `msw2004727/secret-chat`. Work on a branch and open a PR. Do not merge or publish until the user authorizes that specific change; merging to main deploys automatically.
- Run `npm ci`, `npm ci --prefix functions`, `npm test`, and `npm run build` as needed. Node 22 is the cloud runtime.
- Preserve all existing room fingerprints in config.js, including room-5. Do not regenerate appSalt or encryption keys. Never delete database records to repair a source/config issue.
- Preserve LINE application login and review-off anonymous room access. Do not disable LINE App login to work around browser callbacks.
- Location sharing requires explicit opt-in and revocation. Only report once per successful homepage password verification when opted in. Do not introduce silent tracking or device history.
- Deploy chat only to project `daily-notes-7bb64`, Hosting site `daily-notes-7bb64` using the cloud config generated by `npm run build`.
- Root domain `02251121.com` is a separate site `dn-domain-retire` configured in `domain-redirect/firebase.json`. Never include it in chat deployments.
- Never upload .env, room password lists, location private keys, database exports, local backup archives, or cloud credentials. Public Firebase client configuration is expected in config.js.
- Do not expose preview copies backed by production data casually: Firebase Hosting previews share their configured backend. The current checks are offline; an isolated staging backend is not provisioned.
- If app shell files change, update APP_VERSION in app.js, sc-shell in index.html, and VERSION in sw.js together.
- Deployment workflows use short-lived GitHub OIDC credentials limited to main. No production deployment credentials belong in the Codex environment.
